{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (printf "%s-shiftscope" .Release.Name) }}
  labels:
    app.kubernetes.io/name: shiftscope
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
